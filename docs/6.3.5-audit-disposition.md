# Fork 6.3.5 audit disposition

## Scope and lineage

The 2026-09-11 audit is reasonable: F01-F09 identify source/test-contract defects;
H01-H04 identify inconsistencies, not proven runtime deadlocks. This candidate
selectively integrates the separate 6.3.2 customization line into 6.3.4 rather
than reverting the entire tree.

- Current Git base: `d5b681019de87d7f7960d3c0efc2465e9aa8b89c` (6.3.4).
- Earlier release: `dcf01b9ad873337d42696b354adbb6473201d83e` (6.3.2).
- Common base: `b36e0829c6d0140e93cfef2ca599b1b07d4a7797` (upstream 6.3.0).
- Both endpoints have four unique commits. The local reflog starts at the common
  base followed by the four compatibility fixes; it does not establish why that
  starting point was chosen. Missing old customizations were not sequentially
  removed by those four fixes.

The original audit remains unchanged. Its incomplete-repeat snapshot is now
historical: 108 repeated benchmark cases have since completed. Those compare
control with 6.3.4, not 6.3.2 with 6.3.5, and contain qualified approval/protocol
measurement limitations. They are not candidate-release evidence.

## Disposition

| Item | Evidence and 6.3.5 action | Proof boundary |
|---|---|---|
| F01 | Removed conflicting cheap/standard/stronger-model routing from SDD, diagrams, blockers, repair/final review and Codex guidance. Keep parent model intent via supported harness mechanisms. | Contract lint; actual child metadata required for runtime claims. |
| F02 | Shared launcher pins CLI and plugin source, isolates configuration, records requested/observed provenance, and bounds calls/time/cost. Recall and integration share it. Default runner is offline. | Argv stubs plus actual smoke init records. |
| F03 | Oracle and integration prompt distinguish task-brief files from the whole plan. | Fixed positive and negative answers. |
| F04 | Normalize both sides of equivalent workspace paths; retain fixtures with spaces and linked worktrees. | Real temporary Git fixture assertions, not model claims. |
| F05 | Restore bounded navigation; replace hypothetical skill chaining and irreversible escalation with evidence-led scope changes. | Contract tests and narrow-task smoke observations. |
| F06 | Focused, Targeted Behavior, Full Pressure tiers with explicit finite model budgets and no automatic expansion. | Contract tests; no claim of measured pressure-campaign savings. |
| F07 | Independent audit reports and stops; remediation belongs only to already-authorized implementation. | Contract tests and read-only audit smoke. |
| F08 | Focused task proof; full suites only for named integration/repository/delivery gates. Authorized preparation only. | Contract tests; real required verification remains binding. |
| F09 | Release literals removed from dispatch behavior tests; all nine manifests use the registry-driven version check. | Registry regression fixtures and live consistency check. |
| H01 | Final reviewer explicitly prohibits Skill, nested agents, workflow restart and Git/worktree mutation. | Baseline smoke did observe a forbidden child Skill call; candidate result is separate. |
| H02 | Claude blocking foreground waits and asynchronous adapter event waits are separate. | Static consistency; other harness runtimes untested. |
| H03 | Explicit inline execution stays inline and reuses the authorized checkout/disposition. | Static tests plus approved-inline smoke. |
| H04 | Coordinator owns finite model validation; implementers/reviewers do local assigned proof without nested evaluators. | Static consistency; no previously demonstrated deadlock claimed. |

## Preservation matrix

Restored or equivalently implemented: bounded navigation (`5c48a53`), convergence
(`c12edf8`), and validation tiers (`dcf01b9`). Not restored: historical model-tier
selection, machine-level model backstops, unconditional repeated pressure tests,
or automatic audit remediation.

Retained: task briefs, review packages, ledger/recovery, completed-task protection,
batching similar work, scoped read-only task/re-review gates, five repair rounds,
and one final fix wave. Parent model intent and Claude foreground compatibility
remain; omitted parameters alone are not advertised as cross-harness proof.

## Validation

Run `bash tests/claude-code/run-offline-checks.sh` for deterministic checks only.
The native plugin validator passed with its existing warning that root CLAUDE.md
is contributor context, not shipped project instructions. Runtime policy is in
skills, not dependent on that root file.

The bounded live matrix is six predeclared cases per version, one observation per
case, fixed local Astra route, high effort, native Claude Code 2.1.266, 180 seconds
and USD100 per top-level case including children, zero retries. There is one
separate preflight. No hard-6 rerun or hidden model judge is included.

Baseline 6.3.4: five PASS, one FAIL. The failure is a reviewer invoking Skill
against explicit no-Skill instructions. The other five cases already passed;
no behavioral failure is fabricated to justify their contract repairs.

Two frozen smoke measurement limitations are retained, not quietly regraded:
- The scoped-read extractor misses successful `git -C ... show SHA:path` reads.
- Actual root and child message labels can be `gpt-6-astra`, while requested/init
  metadata says `gpt-6-astra[1m]`. Equality of observed root/child labels is evidence,
  but exact child routing remains NOT_VERIFIED under the frozen literal oracle.

Candidate smoke results and the final source identity are recorded separately in
external `superpowers-635-validation-20260912` evidence and summarized at delivery.
Neither static consistency nor a one-sample smoke establishes universal
compliance, comparative performance, or support on untested harnesses.

## Release boundary

All nine registered version fields identify 6.3.5, including this repository's
development marketplace entry. This is an uncommitted candidate, identified by
the base SHA and an external file-content digest; no candidate Git SHA is invented.
The independent Kai marketplace remains pinned to 6.3.2. No commit, push, tag,
marketplace rollout, or other-device installation is part of this change.
