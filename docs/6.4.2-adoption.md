# 6.4.2 candidate adoption matrix

One user-requested development candidate on 6.4.1 source
`e52897bcda6af4ac613a2e417464ad230663b8fa`. Bounded Astra Low evaluation is
complete; this candidate remains a development checkpoint, not a demonstrated
need or guaranteed quality/efficiency benefit. No marketplace promotion.

| Idea | Disposition | Boundary |
| --- | --- | --- |
| Product-outcome convergence lessons | Adapt | General-purpose guidance only; no private implementation, task answers or evaluation oracle. |
| Current usable slice | Adopt in existing brief/goal slots | Entry, user action, observable acceptance, exclusions at project/phase start; no full planning for conversational quick fixes. |
| Critical external dependency reality | Adopt | Early minimum authorized real-boundary validation; distinguish mock, model process and actual service/broker. Explicit simulation phase goals remain valid. |
| Repeated infrastructure-only increments or same-fix loops | Conditional reassessment | Compare next dependency to user outcome; not mechanical stopping, autonomous scope change or repeated approval within existing authorization. |
| Capability status | Existing final summary only | Usable, simulated, unverified or blocked, with evidence/limits; no separate ledger or JSON. |
| 6.4.1 workspace ownership and Native helpers | Retain unchanged | Existing helper identity, stale-evidence and authorization behavior. |
| Tests, safety, parent workflow/model and budget boundaries | Retain | No safety removal, forced reviews/reflections/worktrees/commits, model routing or configuration changes. |

## Evidence boundary

The supplied baseline summary reports both versions passing the real-dependency
case; the no-guidance recovery result is unknown because observer background-task
tracking blocked grading despite task completion. It is not evidence of a 6.4.1
quality failure. No future evaluation results inform this candidate.

Offline RED/GREEN instruction contracts check the four named mechanisms and
existing convergence/dispatch/validation contracts. Bounded real-helper fixtures
protect retained Native behavior; static checks are not consuming-agent proof.
No live calls, new agents, installation or remote configuration are authorized
for implementation. Bootstrap/core/hooks remain byte-identical: added prompt
cost is on-demand text, reported as character deltas, not precision token counts.

The nine existing registry fields use numeric 6.4.2. The uncommitted source
snapshot has a content digest/file map separate from its base Git SHA; it must
not be represented as a new commit. Evaluation copies materialize AGENTS.md as
the same real CLAUDE.md text on both baseline and candidate. Freeze follows
completed offline checks and final source edits; no source edits during live
comparison. README and release-note result updates, if later authorized, remain
separate from the frozen evaluated source.

## Development checkpoint

The initial candidate run passed Golden v4's seven cases and two pre-generated
confirmation instances. Three subsequent, fixed-condition Astra Low rounds
compared no plugin, 6.3.6, 6.4.1 and this candidate; every candidate case passed.
Two no-plugin runs retained unknown functional outcomes due to observer lifecycle
blocks, rather than being relabeled code failures. Shared-subset comparisons
excluded the affected tasks consistently.

Efficiency rankings changed across the repeated rounds. Neither a single winning
round nor three observations establishes stable superiority or broad reliability.
The new cases exercise synthetic integration/recovery contracts, not actual
provider trading or autonomous product-priority judgment. Existing observer and
portable-export limitations are tracked separately in PB. Raw results, costs,
source hashes and detailed comparisons remain in the external evidence tier;
this checkpoint does not rewrite them or promote the marketplace pin.
